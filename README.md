---
# 

---
---
# Freelance Platform Backend

Это backend для сервиса фрилансеров, написанный на Python с использованием FastAPI.

---
## Технологии

- **FastAPI** — быстрый и удобный веб-фреймворк для создания API
    
- **Celery** — для выполнения фоновых задач (пока не готово)
    
- **Redis** — будет использоваться как брокер сообщений для Celery
    
- **SQLAlchemy** — ORM для работы с базой данных
    
- **aiosqlite** — асинхронный драйвер для SQLite
    
- **Poetry** — для управления зависимостями и виртуальным окружением
    

---

## Почему SQLite?

Выбрал SQLite для простоты и скорости разработки:

- Не нужно настраивать отдельный сервер базы данных
    
- Удобно для локальной разработки и тестирования
    
- Все данные хранятся в одном файле — удобно для прототипа
    

---

## Как устроен проект

Проект разделён на части, чтобы код был понятным и удобным для поддержки:

- **Роуты** — принимают HTTP-запросы и отправляют ответы
    
- **Сервисы** — бизнес-логика приложения
    
- **Репозитории** — операции с базой данных
    
- **Unit of Work** — паттерн, который помогает работать с базой данных в рамках одной транзакции, чтобы изменения были атомарными
    
- **Фоновые задачи** — будут использоваться для долгих операций, например, отправки писем (пока не реализованы)
    

---

## Паттерн Unit of Work

Используется для того, чтобы:

- Группировать несколько операций с базой в одной транзакции
    
- Централизованно управлять коммитом и откатом изменений
    
- Избежать проблем с целостностью данных при ошибках
    

---

## Текущий статус

Проект только начинается. Фоновые задачи пока не настроены, но архитектура уже подготовлена для расширения и масштабирования.

В ближайшее время планирую добавить больше функционала и инструкции по запуску проекта у себя на компьютере.

---